
  # فروشگاه آنلاین لباس فابی

  This is a code bundle for فروشگاه آنلاین لباس فابی. The original project is available at https://fabee-store.vercel.app/

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  